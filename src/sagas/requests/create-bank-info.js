import axios from "axios";
import { apiConstants } from "../../constants/api_constants";

export function requestCreateBankInfo() {
  // return axios.post(apiConstants.UPLOAD_PROPERTY_IMAGES, data);
  return { data: { data: "empty" } };
}
